# OOs.Common.CommandLine.Generators

Source generator that emits efficient command-line parsing code.

This package is intended to be consumed as a Roslyn analyzer and provides code-generation support for the `OOs.Common.CommandLine` package.